https://github.com/tangyoha/pyrogram/archive/refs/heads/patch.zip
# 使用最新的 PyYAML 稳定版本 (6.0.3) 或兼容版本
PyYAML>=6.0.1
# 使用最新的 rich 版本
rich>=13.0.0
# 解决 PyTgCrypto 依赖错误
PyTgCrypto>=1.2.11 
# aligo==5.4.0
# 使用 Flask 3.x 及其依赖的 Werkzeug 3.x
Werkzeug>=3.0.0
flask>=3.0.0
ply>=3.11
# 使用 ruamel.yaml 最新版本
ruamel.yaml>=0.18.0
# 使用 flask-login 最新版本
flask-login>=0.6.3
# 使用 pycryptodome 最新版本
pycryptodome>=3.20.0
# 使用 requests 最新版本
requests>=2.32.5